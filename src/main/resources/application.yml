server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/enocaDB
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
